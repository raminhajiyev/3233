trigger:
  branches:
    include:
      - main
  paths:
    include:
      - '*'
    exclude:
      - 'VERSION.txt'
      - 'changelog.md'
      - 'packaging/*'
      - 'documentation/*'

pr:
  branches:
    include:
      - '*'
    exclude:
      - 'currency/*'
  paths:
    include:
      - '*'
    exclude:
      - 'VERSION.txt'
      - 'changelog.md'
      - 'packaging/*'
      - 'documentation/*'

parameters:
- name: testList
  displayName: Test List
  default: all

jobs:
- job: SmokeTests
  steps:
  - script: echo "Running Smoke Tests"
